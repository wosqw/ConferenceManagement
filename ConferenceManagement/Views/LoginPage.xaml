<Page x:Class="ConferenceManagement.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      Title="Вход">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Вход в систему" Margin="0,0,0,16" Style="{StaticResource TitleTextBlockStyle}"/>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="320">
            <TextBlock Text="Email"/>
            <TextBox x:Name="EmailBox" Margin="0,4,0,12" Style="{StaticResource TextBoxStyle}"/>
            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,4,0,16"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                <Button Content="Войти" Width="140" Click="OnLoginClick" Style="{StaticResource AccentButtonStyle}"/>
                <Button Content="Войти как гость" Width="180" Click="OnGuestClick" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>
            <TextBlock x:Name="ErrorText" Foreground="Red" Margin="0,12,0,0"/>
        </StackPanel>
    </Grid>
</Page>