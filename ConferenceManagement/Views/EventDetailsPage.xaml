<Page x:Class="ConferenceManagement.Views.EventDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      Title="Подробности мероприятия">
    <ScrollViewer>
        <StackPanel Margin="16" >
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Image x:Name="Logo" Width="96" Height="96"/>
                <StackPanel>
                    <TextBlock x:Name="TitleText" FontSize="20" FontWeight="Bold"/>
                    <TextBlock x:Name="DatesText"/>
                    <TextBlock x:Name="CityText"/>
                    <TextBlock x:Name="OrganizerText"/>
                </StackPanel>
            </StackPanel>
            <TextBlock Text="Описание" Margin="0,16,0,4" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock x:Name="DescriptionText" TextWrapping="Wrap"/>
            <TextBlock Text="Активности" FontWeight="Bold" Margin="0,16,0,4"/>
            <ItemsControl x:Name="ActivitiesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8" Margin="0,0,0,8" BorderBrush="#DDD" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding StartTime, StringFormat=dd.MM.yyyy HH:mm}"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>